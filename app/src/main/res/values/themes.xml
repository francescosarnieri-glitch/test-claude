<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.CyberSensei" parent="android:Theme.Material.NoActionBar" />

    <!-- Shown for the fraction of a second before Compose takes over. -->
    <style name="Theme.CyberSensei.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.CyberSensei</item>
    </style>

</resources>
