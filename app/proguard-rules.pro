# The app ships no reflection-heavy libraries yet; Hilt and Compose bring their own rules.
# Kotlinx-serialization models will need keep rules when the content loader lands (Fase 1).
-dontwarn org.jetbrains.annotations.**
